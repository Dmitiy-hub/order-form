<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://cdn.linearicons.com/free/1.0.0/icon-font.min.css">
  <link rel="stylesheet" href="/style.css">
  <title>Order</title>
</head>
<body>
<div class="order-wrapper">
      <article class="order-card">
        <div class="btns">
        <a href="/edit/<%= order.id %>" class="order-btn-red lnr lnr-pencil"></a>
        <button class="order-btn-close lnr lnr-cross" data-id="<%= order.id %>"></button> 
      </div> 
        <h2 class="order-name">
          <a href="/order/<%= order.id %>"><%= order.name %></a>
        </h2>
        <p class="order-language"><%= order.language %></p>
        <p class="order-text"><%= order.text %></p>
        <div class="order-info">
          <span><%= order.email %></span>
          <span><%= order.createdAt.toLocaleDateString() %></span>
        </div>
      </article>

</div>  

<script>
  document.addEventListener('click', (e) => {
    const id = e.target?.dataset?.id || null;
    if (id) {
      fetch(`/order/${id}`, {
        method: 'DELETE',
      }).then(() => {
        window.location.href = '/';
      });
    }
  });
</script>
</body>
</html>